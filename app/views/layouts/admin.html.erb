<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Admin - Thoughts" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 min-h-screen">
    <% if current_admin %>
      <nav class="bg-white shadow-sm border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex items-center">
              <%= link_to "Thoughts Admin", admin_root_path, class: "text-xl font-bold text-gray-900" %>
            </div>
            <div class="flex items-center space-x-4">
              <span class="text-gray-600 text-sm"><%= current_admin.email %></span>
              <%= button_to "Logout", admin_session_path, method: :delete, class: "text-gray-600 hover:text-gray-900 text-sm" %>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <% if flash[:notice] %>
        <div class="mb-4 p-4 bg-green-100 border border-green-400 text-green-700 rounded">
          <%= flash[:notice] %>
        </div>
      <% end %>

      <% if flash[:alert] %>
        <div class="mb-4 p-4 bg-red-100 border border-red-400 text-red-700 rounded">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
